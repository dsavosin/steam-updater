# Copy to .env and fill in. The Anthropic SDK reads ANTHROPIC_API_KEY from the
# environment automatically. Load it however you prefer, e.g.:
#   export $(grep -v '^#' .env | xargs)
ANTHROPIC_API_KEY=sk-ant-...
